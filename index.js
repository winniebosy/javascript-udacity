// simple program testing even odd numbers using ternary operator

const input = prompt("Enter a number to determine if is even or odd");
const output = input % 2 === 0 ? "even" : "odd";
alert(`The number is ${output}`);